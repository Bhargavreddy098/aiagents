packages:
  - "packages/*"
  - "apps/*"

# pnpm 10 does not run dependency lifecycle scripts by default.
# Prisma needs its postinstall to emit the client.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - prisma
  - esbuild
