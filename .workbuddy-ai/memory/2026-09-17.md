# 2026-09-17

## NEXS spec set — comparison & verification (read-only pass)

Workspace `C:\Users\bharg\OneDrive\Desktop\agents` holds four markdown files that are
versions/fragments of the same NEXS Agent Control Plane build spec:

- `message-1789586722003.md` (1103 lines) — v1 "deep-research build document". Original draft.
- `PRD.md` (955 lines) — v1 text + Part 6 "production hardening" appendix. **Formatting destroyed** (all 12 fenced code blocks stripped, architecture diagram collapsed to one line).
- `nexs-build-spec.md` (1651 lines) — **v2.0 "gap-closed". The canonical/best artifact.** Superset of the others.
- `gaps.md` (16 lines) — ledger of the 14 gaps v2 closes.

### Verdict
`nexs-build-spec.md` is the source of truth. It is a strict superset of v1 except for four
items that exist only in `PRD.md` Part 6: L2 response cache, LLM context-window guard,
feature flags, reference-URL table.

### Verified findings (do not re-derive)
Extracted the `prisma` fenced block from `nexs-build-spec.md` to a temp dir and ran
`prisma validate` (v5.22.0). Two passes:

1. **As written → 16 errors, all cascading** from three compact one-line blocks that
   Prisma's parser rejects: `generator client { … }`, `datasource db { … }`, and
   `model Tenant { …; …; … }` (Prisma does not accept `;` as a field separator).
   After reformatting those three into normal multi-line blocks → **5 errors**.
2. **Two genuine semantic defects remain in v2's "fully valid" schema:**
   - `Action.approvalId` / `Approval.actionId` both declare `fields`+`references` on a
     1:1 relation → Prisma: "You have to provide it only on one of the two fields."
   - `Run.chatMessages ChatMessage[]` has no opposite relation field; `ChatMessage.runId`
     needs `run Run? @relation(fields: [runId], references: [id])`.
   Fix both before Phase 1, otherwise `prisma migrate dev` fails.

### Other notes
- v1 has 11 literal `@relation(...)` placeholders (invalid Prisma) → v2 has 0.
- v1 references a "§38" event catalog that never existed; v2 defines it as §3.1.
- `PRD.md`'s Part 7 reference table contains a wrong link: pg-boss is `timgit/pg-boss`
  (pgboss.io), not `ross/improved_cron`. Treat that table as unverified.
- User constraint: **never modify `nexs-build-spec.md`.** Analysis passes must stay read-only.
- SHA-256 at time of review: nexs-build-spec.md `2eab65db…9ef6`,
  PRD.md `dcab3e59…b3aa`, gaps.md `2cdacf36…e699`, message-…md `878f3d89…6c76`.
  (Re-verified after the patch pass — all four unchanged.)

### Patch delivered
Created `nexs-build-spec-patch.md` (workspace root, new file — source spec untouched).
Contains Part A (5 schema corrections, verified) + Part B (4 PRD-only sections ported:
L2 cache, context-window guard, feature flags, corrected reference links).

Schema fix that finally validated (after two failed attempts — Prisma requires an opposite
relation field, so you cannot simply delete one side):
- `Action`: drop `approvalId String? @unique` **and** its `@relation(...)`, keep bare
  `approval Approval?` as the back-relation. `Approval.actionId`/`action` stays the owner.
- `ChatMessage`: add `run Run? @relation(fields: [runId], references: [id])`.
- Plus expand the one-line `generator`, `datasource`, and `model Tenant` blocks.
- `DATABASE_URL` must be set (dummy is fine) or `prisma validate` fails at getConfig.

### Link check (HTTP status, 2026-09-17)
Live: modelcontextprotocol.io, zylos.ai, veilock.com, doc.tokenpapa.ai.
**404:** `github.com/ross/improved_cron` (wrong — pg-boss is `timgit/pg-boss` / pgboss.io)
and `github.com/laughingman777/openai-gateway` (dead, no replacement found).

### Open item
Asked the user for standing identity details (their name / what to call me). Not yet answered,
so `BOOTSTRAP.md` at `~/.workbuddy-ai/BOOTSTRAP.md` is still pending — do not delete until
SOUL.md / IDENTITY.md / USER.md are filled in.

### Clarification logged (user misread the link audit)
User asked "so now there is no gateway and pg-boss?" after the 404 report. Worth remembering:
`PRD.md` Part 7 is a **reading list** ("Topic → URL"), not a dependency manifest. Two dead
links there remove nothing:
- pg-boss = live npm package (`timgit/pg-boss`, pgboss.io). Only the printed URL was wrong.
  Still the locked §1.4 scheduling choice, still the Redis-free option.
- Model Gateway = **our own internal service** at `apps/server/src/services/models/`
  (defined in §0 rule 3, §1.3, §4-PHASE3.4). Not a dependency; nothing to install. The dead
  "OpenAI gateway" link is coincidental name collision only.
Added this clarification to `nexs-build-spec-patch.md` §B4 so it can't be misread again.
